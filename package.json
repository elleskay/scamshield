{
  "name": "mobile-platform",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript platform template for shipping React Native (Expo) apps with a NestJS API to AWS serverless.",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "echo 'Platform template has no app source. Apps add their own typecheck script.'",
    "lint": "echo 'Platform template has no app source. Apps add their own lint script.'",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "@commitlint/types": "^21.0.1",
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.3.0",
    "typescript": "^5.4.0",
    "typescript-eslint": "^8.0.0"
  }
}
